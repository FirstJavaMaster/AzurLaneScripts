#
# 配置文件
[default]

# ADB连接地址和端口号。
#
# 必需配置，程序会寻找此地址的设备，找不到则自动终止。
# 不同模拟器默认端口号不同，可参考 https://github.com/FirstJavaMaster/AzurLaneScripts/blob/master/README.md。
adb_host_port = 127.0.0.1:21503

# 自动退役
auto_retire = True

# 最多通关次数
max_stage_fight_times = 15